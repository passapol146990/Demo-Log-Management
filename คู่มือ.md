# คู่มือการรันและทดสอบระบบ Log Management

คู่มือนี้เขียนให้ทำตามได้ทีละขั้น ไม่ต้องเข้าใจโค้ดก็รันได้

---

## ภาพรวม

```
1. เตรียม .env (ทำครั้งแรกครั้งเดียว)
2. รัน docker compose up -d  →  ปล่อยรันค้างไว้ตลอด
3. รอ 30-60 วินาที ให้ทุก service พร้อม
4. เปิดเว็บ login แล้วทดสอบผ่านหน้าจอ
5. ยิง sample data เข้าระบบด้วย script ที่เตรียมไว้ให้
6. เช็คผลลัพธ์ตาม checklist
7. (ถ้าต้องการ) รัน automated test แยกต่างหาก
```

---

## ขั้นตอนที่ 1: เตรียม Environment

ทำครั้งแรกครั้งเดียวเท่านั้น ถ้ามีไฟล์ `.env` อยู่แล้วข้ามขั้นตอนนี้ไปได้เลย

```bash
cd /home/passa/Documents/Demo-Log-Management
cp .env.example .env
```

---

## ขั้นตอนที่ 2: เปิดระบบด้วย Docker

รันคำสั่งนี้ **ครั้งเดียว** แล้วปล่อยให้รันค้างอยู่เบื้องหลัง ไม่ต้องปิด ไม่ต้องกด Ctrl+C:

```bash
cd /home/passa/Documents/Demo-Log-Management
docker compose up -d
```

> `-d` แปลว่ารันแบบ background ไม่บล็อกหน้าจอ ปิด terminal ได้โดยไม่กระทบระบบ

รอประมาณ 30-60 วินาที แล้วเช็คสถานะ:

```bash
docker compose ps
```

**ต้องเห็นครบ 4 service สถานะ `Up` แบบนี้:**

| Service | ทำหน้าที่อะไร | ต้องรันค้างไหม |
|---|---|---|
| `opensearch` | เก็บและค้นหา log | ✅ ต้องรันตลอด |
| `backend` | เว็บ + API (port 3000) | ✅ ต้องรันตลอด |
| `vector` | รับ syslog (port 514) | ✅ ต้องรันตลอด |
| `worker` | เช็ค alert ทุก 60 วิ + ลบ log เก่าทุก 24 ชม. | ✅ ต้องรันตลอด |

ไม่มีลำดับที่ต้องรันแยกทีละตัว คำสั่งเดียวจบ ระบบจะจัดลำดับให้เองว่าอะไรต้องพร้อมก่อน

---

## ขั้นตอนที่ 3: เข้าใช้งานผ่านหน้าเว็บ

### เปิดเบราว์เซอร์ไปที่

```
http://localhost:3000/login
```

### Login ด้วยบัญชีทดสอบ (มีให้ 4 บัญชี)

| Email | Password | สิทธิ์ | สังกัด |
|---|---|---|---|
| `admin@demoA` | `password123` | admin | demoA |
| `viewer@demoA` | `password123` | viewer | demoA |
| `admin@demoB` | `password123` | admin | demoB |
| `viewer@demoB` | `password123` | viewer | demoB |

**เริ่มต้นให้ login ด้วย `admin@demoA` ก่อน** จะเข้าหน้า Dashboard อัตโนมัติ

### หน้าที่ต้องดู

| หน้า | URL | ควรเห็นอะไร |
|---|---|---|
| **Dashboard** | `/dashboard` | สรุปตัวเลข, กราฟ Top Source/User/IP, กราฟเวลา (Timeline), กราฟความรุนแรง (Severity) |
| **Log Search** | `/dashboard/search` | ช่องค้นหา + filter แหล่งข้อมูล/วันที่ + ตารางผลลัพธ์ |
| **Alerts** | `/dashboard/alerts` | รายการแจ้งเตือนที่เกิดขึ้น |

ตอนนี้หน้าจอน่าจะยังว่างเพราะยังไม่มีข้อมูล ไปขั้นตอนถัดไปเพื่อยิงข้อมูลทดสอบเข้าระบบ

---

## ขั้นตอนที่ 4: ยิงข้อมูลทดสอบเข้าระบบ

เปิด **terminal ใหม่อีกหน้าต่าง** (ไม่ต้องปิดหน้าต่างที่รัน docker) แล้วรันตามลำดับ:

### 4.1 ทดสอบว่า ingest รับ log ได้ทุกประเภท

ยิงตัวอย่างแหล่งละ 1 event ผ่าน `/api/ingest` เพื่อเช็คว่า normalizer ของทุกแหล่งทำงาน (api, firewall, network, crowdstrike, aws, m365, ad):

```bash
cd /home/passa/Documents/Demo-Log-Management
python3 samples/scripts/1_send_examples.py
```

ผลลัพธ์ที่ควรเห็น: ทุกบรรทัดขึ้น `HTTP 200` และปิดท้ายด้วย `Total: 7 success, 0 failed`

### 4.2 ส่ง Syslog (จำลอง Firewall log ผ่าน port 514)

```bash
python3 samples/scripts/2_send_syslog.py
```

จะเห็นข้อความส่งทีละบรรทัดจนครบ

### 4.3 ส่ง JSON logs ผ่าน API (ครบทุกแหล่ง: api, crowdstrike, aws, m365, ad)

```bash
python3 samples/scripts/3_post_logs.py
```

ผลลัพธ์ที่ควรเห็น:
```
api: 7 success, 0 failed
crowdstrike: 6 success, 0 failed
aws: 6 success, 0 failed
m365: 6 success, 0 failed
ad: 7 success, 0 failed
Total: 32 success, 0 failed
```

### 4.3.1 ยิงข้อมูลแบบ Batch (ทีเดียวทั้งไฟล์)

นอกจากยิงทีละ record แบบข้างบน `/api/ingest` ยังรองรับการส่ง log หลายรายการมาในคำขอเดียว
ด้วยรูปแบบ `{ "logs": [ {...}, {...}, ... ] }` — แต่ละรายการจะถูกตรวจสอบและ normalize แยกกัน
ถ้าบางรายการผิดพลาดก็ยังประมวลผลรายการที่เหลือต่อได้ (partial success) เหมาะสำหรับกรณีอยากยิง
ทั้งไฟล์ sample ทีเดียวแทนที่จะวนยิงทีละ record เช่น:

```bash
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@demoA","password":"password123"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

jq '{logs: .}' samples/logs/api_events.json | curl -s -X POST http://localhost:3000/api/ingest \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d @- -w "\nHTTP:%{http_code}\n"
```

ผลลัพธ์จะเป็นสรุปจำนวนที่สำเร็จ/ล้มเหลวต่อรายการ เช่น `{"batch":true,"total":7,"succeeded":7,"failed":0,"results":[...]}`

### 4.4 กลับไปดูหน้าเว็บอีกครั้ง

รีเฟรชหน้า `http://localhost:3000/dashboard` — ตอนนี้กราฟและตารางควรมีข้อมูลเต็มแล้ว

---

## ขั้นตอนที่ 5: ทดสอบระบบแจ้งเตือน (Alert)

ข้อมูลตัวอย่างมี "login ผิดพลาด 5 ครั้งติดจาก IP เดียวกัน" ซ้อนไว้อยู่แล้ว เพื่อทดสอบกฎแจ้งเตือนอัตโนมัติ

**รอ 60 วินาที** (ระบบเช็คทุก 1 นาที) แล้วเปิด:

```
http://localhost:3000/dashboard/alerts
```

ควรเห็นแจ้งเตือนแบบนี้:
> Login Failures — 5+ login failures from IP 203.0.113.77 within 5 minutes

ถ้ายังไม่เห็น ลองยิง `python3 samples/scripts/3_post_logs.py` ใหม่อีกรอบแล้วรอ 60 วิอีกครั้ง

หรือจะสาธิตให้ชัดเจนด้วยสคริปต์ที่เปิด rule แล้วยิง 5 events จาก IP เดียวกันให้เลยก็ได้:

```bash
python3 samples/scripts/4_demo_login_failure_alert.py
```

---

## ขั้นตอนที่ 6: ทดสอบระบบความปลอดภัย (ไม่บังคับ แต่แนะนำให้ลอง)

### ทดสอบว่า Viewer สั่งเพิ่มข้อมูลไม่ได้ (ต้องถูกปฏิเสธ)

```bash
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"viewer@demoA","password":"password123"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

curl -s -X POST http://localhost:3000/api/ingest \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"source":"api","tenant":"demoA"}' -w "\nHTTP:%{http_code}\n"
```

ผลลัพธ์ที่ถูกต้อง: `{"error":"Forbidden"}` และ `HTTP:403`

### ทดสอบว่าแต่ละบริษัท (tenant) เห็นข้อมูลแยกกัน

Login ด้วย `admin@demoB` แล้วเปิด `/dashboard/search` — ต้องเห็นข้อมูล**ว่างเปล่า** เพราะข้อมูลตัวอย่างทั้งหมดเป็นของ demoA เท่านั้น

### ทดสอบว่าเข้าหน้า Dashboard โดยไม่ Login ไม่ได้

เปิดหน้าต่าง Incognito/Private แล้วพิมพ์ `http://localhost:3000/dashboard` ตรงๆ โดยไม่ login ก่อน — ต้องถูกเด้งกลับไปหน้า login อัตโนมัติ

---

## ขั้นตอนที่ 7: รัน Automated Test (ไม่บังคับ)

ไม่ต้องหยุด docker เปิด terminal ใหม่แล้วรัน:

```bash
cd /home/passa/Documents/Demo-Log-Management/backend
npx jest
```

ผลลัพธ์ที่ควรเห็น: **`Tests: 40 passed, 40 total`**

---

## สรุปคำสั่งทั้งหมด (สำหรับก็อปวาง)

```bash
# 1. เตรียม env (ครั้งแรกเท่านั้น)
cd /home/passa/Documents/Demo-Log-Management
cp .env.example .env

# 2. เปิดระบบ (รันค้างไว้)
docker compose up -d

# 3. เช็คสถานะ (รอ 30-60 วิก่อนเช็ค)
docker compose ps

# 4. เปิดเบราว์เซอร์
#    http://localhost:3000/login
#    login: admin@demoA / password123

# 5. ยิงข้อมูลทดสอบ (รันตามลำดับ)
python3 samples/scripts/1_send_examples.py
python3 samples/scripts/2_send_syslog.py
python3 samples/scripts/3_post_logs.py
# (สาธิต alert) python3 samples/scripts/4_demo_login_failure_alert.py

# 6. รอ 60 วิ แล้วดูหน้า alert
#    http://localhost:3000/dashboard/alerts

# 7. รัน test (ไม่บังคับ)
cd backend && npx jest

# --- เมื่อทดสอบเสร็จทั้งหมดแล้ว ---
cd /home/passa/Documents/Demo-Log-Management
docker compose down
```

---

## ตารางเช็คลิสต์สรุป (Checklist)

- [x] `docker compose ps` เห็นครบ 4 service = `Up`
- [x] เปิด `/login` ได้ และ login สำเร็จด้วย `admin@demoA`
- [x] `/dashboard` แสดงกราฟ Top N + Timeline + Severity หลังยิงข้อมูล
- [ ] `/dashboard/search` filter ตาม source/วันที่ได้จริง
- [ ] `2_send_syslog.py` รันแล้วไม่มี error
- [ ] `3_post_logs.py` รันแล้วได้ `Total: 32 success, 0 failed`
- [ ] `/dashboard/alerts` มี alert ขึ้นหลังรอ 60 วิ
- [ ] Viewer สั่ง ingest แล้วได้ `403 Forbidden`
- [ ] demoB มองไม่เห็น log ของ demoA (tenant แยกกันจริง)
- [ ] เข้า `/dashboard` โดยไม่ login ถูกเด้งไป `/login`
- [ ] `npx jest` ได้ `40 passed, 40 total`

---

## คำถามที่พบบ่อย

**Q: ต้องรันคำสั่งไหนก่อนไหนหลังบ้าง?**
A: มีแค่ `docker compose up -d` ที่ต้องรันก่อนสุดและรันค้างไว้ตลอด ที่เหลือ (ยิง sample data, เปิดเว็บ, รัน test) จะรันตอนไหนก็ได้หลังจากนั้น ไม่มีลำดับตายตัว

**Q: terminal ที่รัน docker ปิดได้ไหม?**
A: ปิดได้ เพราะใช้ `-d` (background mode) container จะรันต่อเบื้องหลังแม้ปิด terminal

**Q: ทำไม alert ไม่ขึ้นทันทีหลังยิงข้อมูล?**
A: ระบบเช็ค alert ทุก 60 วินาทีเท่านั้น (ไม่ใช่ real-time) ต้องรอ 1 นาทีเสมอ

**Q: อยากดู log การทำงานของระบบสด ๆ ทำยังไง?**
A: `docker compose logs -f backend` (เปลี่ยนชื่อ service ตามที่ต้องการดู) กด `Ctrl+C` เพื่อหยุดดู log เฉยๆ ไม่กระทบระบบ

**Q: อยากหยุดระบบทั้งหมดทำยังไง?**
A: `docker compose down` (รันที่ root ของโปรเจกต์)

**Q: OpenSearch password คืออะไร (เผื่อต้องใช้ curl ตรงเข้า OpenSearch)?**
A: ดูได้จากไฟล์ `.env` ตัวแปร `OPENSEARCH_PASSWORD`
